#include <stdio.h>

/**
 * main -  program that prints using puts function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts( "\"Programming is like building a multilingual puzzle" );
return (0);
}
